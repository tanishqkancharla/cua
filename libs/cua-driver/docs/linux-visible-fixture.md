# Visible GTK3 setup for canonical X11 tests

Canonical CI34558519742 at2e81b exposes hidden fixture assumptions after visible-only accessibility observations. The native GTK3 scroll traces visibly advance inner content (line03/line07), but scroll_offset is absent before and after because its label is below the default window viewport. Popover controls and exit are likewise below the viewport. The tests previously relied on offscreen accessibility nodes. The original failed results are preserved.

Scope: drive the owned X11 fixture into a usable visible state using public screen-size/window-frame actions before recording and before the background sentinel is established. Verify returned geometry independently and require the original controls/state markers in fresh observations. Apply only to affected scroll, popover and AX roster cases. Keep tested actions and original outcome assertions unchanged; missing scroll markers must be explicit setup/observation failures instead of defaulting to zero. No product visibility-filter changes, fixture mocks or hidden-state oracles.

Acceptance: rerun affected canonical rows with fresh visible controls and all existing outcome/focus/z-order/cursor/no-leaked-input oracles; preserve failures and recordings. X11 resizing does not certify Wayland, whose geometry protocol limitation remains explicit. Then recertify affected full lanes before readiness. This test-only draft stacks on the screen-origin candidate and is separate from the pixel-focus refusal product correction. Issues are disabled in this fork; this draft is the problem record.

Implementation: the shared test helper first observes required controls and returns without resizing when they are already present. Otherwise it reads the real screen size, makes one public exact-window resize request (800×1000 at20,20 on the canonical1920×1080 desktop), and independently polls `list_windows` for matching PID/window and geometry within2pixels. Only observation is retried. Fresh accessibility state must then contain the original required markers. Geometry or visibility failure is a setup failure, not a skipped/passing scroll.

The setup hook runs after owned launch and before behavior recording or background-sentinel establishment. Only scroll/popover catalog rows, the full AX roster case, and desktop-scope scroll use it. Existing tested actions, positive-offset assertions and background oracles remain unchanged. Missing desktop-scroll offset readings are explicit errors on X11; native Wayland retains its previous setup/marker behavior and receives no geometry mutation. The original560×720 app default is unchanged for all other rows.

Local verification: rustfmt and `git diff --check` pass. The Linux-gated integration targets have not been compiled or run locally on macOS; exact-source Linux CI and real desktop outcomes remain required. No canonical failures have been relabeled.


Focused CI34560340210 at exact4815d9030 passes all eight affected canonical rows. Independent artifact checks verify exact driver/workflow/test identities, unique expected cases, original oracle sets and retained nonempty videos. Native AX scroll markers move0→84, foreground pixel scroll0→161; background pixel scroll correctly refuses with all focus/z-order/cursor/no-leaked-input/fixture oracles. The original tests/actions are unchanged apart from visible setup and explicit missing-marker errors. Full release-lane recertification of the combined candidate remains pending; no agent result was rewritten.
