# Preserve the selected Calc cell across consecutive edits

OpenSky's frozen Linux release evaluation fills the requested blanks but also overwrites four existing student names. A real public-SDK regression using the original OSWorld workbook reproduces the problem in two edits: click C10, type, Enter, click the retained C11 index, type, Enter, save. C10 is written, C11 remains blank, and C12 is overwritten. A visible-name-box control changes only C10/C11. Both owned app groups and containers exit.

Screenshots isolate the error to the second click: C11 is selected after the first Enter, then C12 is selected before the second typing action. The requested cell's AT-SPI identity and TableCell Position remain unchanged. Its reported Screen coordinates change after editing while Window coordinates remain unchanged; the current X11 rebase then adds another offset. Parent-table point lookup shares the initial stale coordinate system, so it cannot independently validate physical targeting. A blanket offset removal or hardcoded tolerance would not be justified.

Scope: investigate a native X11 single-cell selection route for an unmodified single indexed click through the owning table's advertised Selection interface. Preserve exact window/token/cell identity, modifiers and multi-click semantics, and never replay after uncertain mutation. No product change is yet validated. Issues are disabled in this fork; the linked draft PR serves as the problem record.

Acceptance requires the unchanged public-SDK regression and name-box control to save only C10/C11 with all other values and sheet order unchanged. Check existing selections, keyboard focus, and multi-selection behavior rather than trusting a successful SelectChild acknowledgment. Retain initial failures. Run affected canonical Linux certification on the stable candidate, existing indexed-click and focus regressions, and displaced Code/font controls; do not alter the frozen paid campaign or claim macOS/Windows/Wayland verification from X11 evidence.

Evidence: driver403572f34dfbdc83b17bda7fe4c351060be32785, release binary e65549f1a2d77d168581a2c2cc84585f90b51748a1cf85c2cf43bfca7a49ba85; public SDK runtime2d8879fcb40861ac8f95fdcd0171642e3a19a635. Tests and fixture diagnostics are on the OpenSky fork's codex/linux-fill-preservation branch. The implementation workstream is based on merged e25f348b3e0ca9221130e1df8e792ca857cc0505, whose executable tree matches the tested driver. Diagnostic image additions and probe failures are recorded separately from product acceptance.

Candidate30189c6 compiled (77 focused checks) but failed unchanged FILL-L01; FILL-L02 passed. The final screenshot shows C10:C11 marked while the first text went into A1. No successful save is claimed for the failed test. Both owned groups exited and the container was removed. SelectChild marks cells without moving the typing cursor; a successful acknowledgment and exclusive selection alone are insufficient.

The next candidate uses the retained cell's Component.GrabFocus, verifies its Focused state and exact X11 focus, and only handles LibreOffice spreadsheet descendants with no existing marked range. Merged cells and existing selections keep their existing pointer path before any semantic mutation. It does not clear ranges or append a second action after uncertain delivery. This is still unvalidated. LibreOffice24.2.7.2 source: [cell grabFocus and IsFocused](https://github.com/LibreOffice/core/blob/libreoffice-24.2.7.2/sc/source/ui/Accessibility/AccessibleCell.cxx) and [table selection](https://github.com/LibreOffice/core/blob/libreoffice-24.2.7.2/sc/source/ui/Accessibility/AccessibleSpreadsheet.cxx).

The15adb focus candidate passed original FILL-L01/L02 but failed L03/L04: name-box focus remains in the field after GrabFocus, and the marked-range pointer path saves C11 instead of C10. Both failures and owned cleanup are retained. The current candidate removes both semantic experiments completely. It instead applies a Calc-only, per-axis Screen/Window translation correction, requiring exact spreadsheet ancestry, valid equal-size extents and stable Screen→Window→Screen samples. Equal Screen/Window origins keep the prior rebase. Distinct origins suppress only the already-translated axis. No focus, clear-selection, keyboard, or additional pointer input is introduced.

This is a compatibility rule supported by retained Calc geometry and LibreOffice grid-window source, not a general coordinate guarantee. All five public SDK cases, a moved-window counterexample, and existing Code/font/canonical Linux controls remain necessary before promotion.

Candidate d3005c0c3604c989e9f53e996e3e56f46173f9e3 built successfully in CI34574603781 with 78 focused checks. Remote run `calc-geometry-six-after-01` passes FILL-L01–L05, including name-box focus, marked range and merged-cell preservation. FILL-L06 exposes a remaining coordinate error after restoring/moving the window: client origin (201,118), size960×700; saved cells C9/C11 change instead of C10/C11. Whole-workbook comparison and merged ranges were independently read, all six owned groups exited and container inventory is empty. Keep the draft unaccepted; retain the failed moved-window case and diagnose its geometry before further candidate changes. Binary SHA256: b1ca737e5fa6c20991efe95dd77e5845ec6aaf93ba827c4c737b03c59bb6aca0.
